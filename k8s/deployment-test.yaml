---
# RavenClaw K8s Test Deployment
# Uses hostNetwork for local LiteLLM access during verification
# This is a TEST variant — for production, use deployment.yaml with in-cluster LiteLLM
apiVersion: v1
kind: Namespace
metadata:
  name: ravenclaw-test
  labels:
    app.kubernetes.io/name: ravenclaw
    app.kubernetes.io/part-of: ravenclaw-test

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ravenclaw-config
  namespace: ravenclaw-test
data:
  ravenclaw.toml: |
    [llm]
    endpoint = "http://host.docker.internal:4000"
    model = "best-coding"
    timeout_secs = 30

    [security]
    require_tls = false
    audit_log = true

    [runtime]
    workdir = "/workspace"
    max_agents = 3
    health_interval_secs = 10

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ravenclaw
  namespace: ravenclaw-test
  labels:
    app.kubernetes.io/name: ravenclaw
    app.kubernetes.io/component: agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ravenclaw
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ravenclaw
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
        - name: ravenclaw
          image: ravenclaw-verify:latest
          imagePullPolicy: IfNotPresent
          args:
            - "--serve"
            - "--config"
            - "/config/ravenclaw.toml"
          env:
            - name: RUST_LOG
              value: "info"
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: workspace
              mountPath: /workspace
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsUser: 65532
            runAsGroup: 65532
            capabilities:
              drop:
                - ALL
      volumes:
        - name: config
          configMap:
            name: ravenclaw-config
        - name: workspace
          emptyDir:
            sizeLimit: 512Mi
      terminationGracePeriodSeconds: 15
